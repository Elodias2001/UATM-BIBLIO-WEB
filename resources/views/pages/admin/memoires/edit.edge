@layout.admin({ title: `Modifier - ${memory.title}` })

@slot('content')
  <div class="min-h-screen bg-neutral-50">
    <!-- Header -->
    <header class="bg-white border-b border-neutral-200">
      <div class="container mx-auto px-4 py-4">
        <div class="flex items-center justify-between">
          <div>
            <h1 class="text-2xl font-bold text-neutral-900">Modifier le mémoire</h1>
            <p class="text-neutral-600">{{ memory.title }}</p>
          </div>
          <div class="flex items-center gap-3">
            <a href="{{route('admin.memories.show', { id: memory.id })}}" class="text-sm text-neutral-600 hover:text-primary-600 transition-colors">
              <i class="fa-solid fa-arrow-left mr-1"></i>
              Retour aux détails
            </a>
          </div>
        </div>
      </div>
    </header>

    <div class="container mx-auto px-4 py-8">
      <div class="max-w-4xl mx-auto">
        <!-- Flash Messages -->
        @if(flashMessages.get('error'))
          <div class="mb-6 rounded-md border border-error-200 bg-error-50 text-error-700 p-4">
            <div class="flex">
              <i class="fa-solid fa-exclamation-triangle text-error-400 mr-3"></i>
              <div>{{ flashMessages.get('error') }}</div>
            </div>
          </div>
        @endif

        <!-- Form -->
        <div class="bg-white rounded-lg border border-neutral-200 p-6">
          <form action="{{route('admin.memories.update', { id: memory.id })}}" method="POST" class="space-y-6">
            {{ csrfField() }}
            @method('PUT')

            <!-- Title -->
            <div>
              <label for="title" class="block text-sm font-medium text-neutral-700 mb-2">
                Titre du mémoire <span class="text-error-600">*</span>
              </label>
              <input type="text" id="title" name="title" required
                     value="{{ memory.title }}"
                     class="w-full px-3 py-2 border border-neutral-300 rounded-md focus:outline-none focus:ring-2 focus:ring-primary-300 focus:border-primary-500"
                     placeholder="Titre du mémoire ou de la thèse" />
            </div>

            <!-- Authors and Year -->
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
              <div>
                <label for="authors" class="block text-sm font-medium text-neutral-700 mb-2">
                  Auteurs
                </label>
                <input type="text" id="authors" name="authors"
                       value="{{ memory.authors || '' }}"
                       class="w-full px-3 py-2 border border-neutral-300 rounded-md focus:outline-none focus:ring-2 focus:ring-primary-300 focus:border-primary-500"
                       placeholder="Nom, Prénom (séparés par des virgules)" />
              </div>
              <div>
                <label for="year" class="block text-sm font-medium text-neutral-700 mb-2">
                  Année
                </label>
                <input type="number" id="year" name="year" min="1900" max="{{ new Date().getFullYear() + 1 }}"
                       value="{{ memory.year || '' }}"
                       class="w-full px-3 py-2 border border-neutral-300 rounded-md focus:outline-none focus:ring-2 focus:ring-primary-300 focus:border-primary-500"
                       placeholder="2024" />
              </div>
            </div>

            <!-- Filiere and Keywords -->
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
              <div>
                <label for="filiere" class="block text-sm font-medium text-neutral-700 mb-2">
                  Filière
                </label>
                <select id="filiere" name="filiere"
                        class="w-full px-3 py-2 border border-neutral-300 rounded-md focus:outline-none focus:ring-2 focus:ring-primary-300 focus:border-primary-500">
                  <option value="">Sélectionner une filière</option>
                  <option value="Informatique" {{ memory.filiere === 'Informatique' ? 'selected' : '' }}>Informatique</option>
                  <option value="Gestion" {{ memory.filiere === 'Gestion' ? 'selected' : '' }}>Gestion</option>
                  <option value="Sciences" {{ memory.filiere === 'Sciences' ? 'selected' : '' }}>Sciences</option>
                  <option value="Économie" {{ memory.filiere === 'Économie' ? 'selected' : '' }}>Économie</option>
                  <option value="Droit" {{ memory.filiere === 'Droit' ? 'selected' : '' }}>Droit</option>
                  <option value="Autre" {{ memory.filiere === 'Autre' ? 'selected' : '' }}>Autre</option>
                </select>
              </div>
              <div>
                <label for="keywords" class="block text-sm font-medium text-neutral-700 mb-2">
                  Mots-clés
                </label>
                <input type="text" id="keywords" name="keywords"
                       value="{{ memory.keywords || '' }}"
                       class="w-full px-3 py-2 border border-neutral-300 rounded-md focus:outline-none focus:ring-2 focus:ring-primary-300 focus:border-primary-500"
                       placeholder="Mots-clés séparés par des virgules" />
                <p class="text-xs text-neutral-500 mt-1">Ex: IA, machine learning, réseaux</p>
              </div>
            </div>

            <!-- Abstract -->
            <div>
              <label for="abstract" class="block text-sm font-medium text-neutral-700 mb-2">
                Résumé / Abstract
              </label>
              <textarea id="abstract" name="abstract" rows="4"
                        class="w-full px-3 py-2 border border-neutral-300 rounded-md focus:outline-none focus:ring-2 focus:ring-primary-300 focus:border-primary-500"
                        placeholder="Résumé du mémoire ou de la thèse...">{{ memory.abstract || '' }}</textarea>
            </div>

            <!-- File Path -->
            <div>
              <label for="filePath" class="block text-sm font-medium text-neutral-700 mb-2">
                Chemin du fichier
              </label>
              <input type="text" id="filePath" name="filePath"
                     value="{{ memory.filePath || '' }}"
                     class="w-full px-3 py-2 border border-neutral-300 rounded-md focus:outline-none focus:ring-2 focus:ring-primary-300 focus:border-primary-500"
                     placeholder="/memories/nom-du-fichier.pdf" />
              <p class="text-xs text-neutral-500 mt-1">Chemin relatif vers le fichier PDF du mémoire</p>
            </div>

            <!-- Actions -->
            <div class="flex items-center justify-end gap-4 pt-6 border-t border-neutral-200">
              <a href="{{route('admin.memories.show', { id: memory.id })}}" class="px-4 py-2 text-sm text-neutral-600 hover:text-neutral-900 border border-neutral-300 rounded-md transition-colors">
                Annuler
              </a>
              <button type="submit" class="inline-flex items-center gap-2 px-6 py-2 bg-accent-600 text-white rounded-md hover:bg-accent-700 transition-colors">
                <i class="fa-solid fa-save"></i>
                Mettre à jour
              </button>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>
@endslot

@end
