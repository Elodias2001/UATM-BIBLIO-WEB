@layout.admin({ title: "Ajouter un mémoire" })

@slot('content')
  <div class="min-h-screen bg-neutral-50">
    <!-- Header -->
    <header class="bg-white border-b border-neutral-200">
      <div class="container mx-auto px-4 py-4">
        <div class="flex items-center justify-between">
          <div>
            <h1 class="text-2xl font-bold text-neutral-900">Ajouter un mémoire</h1>
            <p class="text-neutral-600">Créer une nouvelle entrée dans la bibliothèque</p>
          </div>
          <div class="flex items-center gap-3">
            <a href="{{route('admin.memories.index')}}" class="text-sm text-neutral-600 hover:text-primary-600 transition-colors">
              <i class="fa-solid fa-arrow-left mr-1"></i>
              Retour à la liste
            </a>
          </div>
        </div>
      </div>
    </header>

    <div class="container mx-auto px-4 py-8">
      <div class="max-w-4xl mx-auto">
        <!-- Flash Messages -->
        @if(flashMessages.get('error'))
          <div class="mb-6 rounded-md border border-error-200 bg-error-50 text-error-700 p-4">
            <div class="flex">
              <i class="fa-solid fa-exclamation-triangle text-error-400 mr-3"></i>
              <div>{{ flashMessages.get('error') }}</div>
            </div>
          </div>
        @endif

        <!-- Form -->
        <div class="bg-white rounded-lg border border-neutral-200 p-6">
          <form action="{{route('admin.memories.store')}}" method="POST" class="space-y-6">
            {{ csrfField() }}

            <!-- Title -->
            <div>
              <label for="title" class="block text-sm font-medium text-neutral-700 mb-2">
                Titre du mémoire <span class="text-error-600">*</span>
              </label>
              <input type="text" id="title" name="title" required
                     class="w-full px-3 py-2 border border-neutral-300 rounded-md focus:outline-none focus:ring-2 focus:ring-primary-300 focus:border-primary-500"
                     placeholder="Titre du mémoire ou de la thèse" />
            </div>

            <!-- Authors and Year -->
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
              <div>
                <label for="authors" class="block text-sm font-medium text-neutral-700 mb-2">
                  Auteurs
                </label>
                <input type="text" id="authors" name="authors"
                       class="w-full px-3 py-2 border border-neutral-300 rounded-md focus:outline-none focus:ring-2 focus:ring-primary-300 focus:border-primary-500"
                       placeholder="Nom, Prénom (séparés par des virgules)" />
              </div>
              <div>
                <label for="year" class="block text-sm font-medium text-neutral-700 mb-2">
                  Année
                </label>
                <input type="number" id="year" name="year" min="1900" max="{{ new Date().getFullYear() + 1 }}"
                       class="w-full px-3 py-2 border border-neutral-300 rounded-md focus:outline-none focus:ring-2 focus:ring-primary-300 focus:border-primary-500"
                       placeholder="2024" />
              </div>
            </div>

            <!-- Filiere and Keywords -->
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
              <div>
                <label for="filiere" class="block text-sm font-medium text-neutral-700 mb-2">
                  Filière
                </label>
                <select id="filiere" name="filiere"
                        class="w-full px-3 py-2 border border-neutral-300 rounded-md focus:outline-none focus:ring-2 focus:ring-primary-300 focus:border-primary-500">
                  <option value="">Sélectionner une filière</option>
                  <option value="Informatique">Informatique</option>
                  <option value="Gestion">Gestion</option>
                  <option value="Sciences">Sciences</option>
                  <option value="Économie">Économie</option>
                  <option value="Droit">Droit</option>
                  <option value="Autre">Autre</option>
                </select>
              </div>
              <div>
                <label for="keywords" class="block text-sm font-medium text-neutral-700 mb-2">
                  Mots-clés
                </label>
                <input type="text" id="keywords" name="keywords"
                       class="w-full px-3 py-2 border border-neutral-300 rounded-md focus:outline-none focus:ring-2 focus:ring-primary-300 focus:border-primary-500"
                       placeholder="Mots-clés séparés par des virgules" />
                <p class="text-xs text-neutral-500 mt-1">Ex: IA, machine learning, réseaux</p>
              </div>
            </div>

            <!-- Abstract -->
            <div>
              <label for="abstract" class="block text-sm font-medium text-neutral-700 mb-2">
                Résumé / Abstract
              </label>
              <textarea id="abstract" name="abstract" rows="4"
                        class="w-full px-3 py-2 border border-neutral-300 rounded-md focus:outline-none focus:ring-2 focus:ring-primary-300 focus:border-primary-500"
                        placeholder="Résumé du mémoire ou de la thèse..."></textarea>
            </div>

            <!-- File Upload -->
            <div>
              <label for="file" class="block text-sm font-medium text-neutral-700 mb-2">
                Fichier PDF du mémoire
              </label>
              <div class="mt-1 flex justify-center px-6 pt-5 pb-6 border-2 border-neutral-300 border-dashed rounded-md hover:border-primary-400 transition-colors">
                <div class="space-y-1 text-center">
                  <i class="fa-solid fa-cloud-upload-alt text-4xl text-neutral-400"></i>
                  <div class="flex text-sm text-neutral-600">
                    <label for="file" class="relative cursor-pointer bg-white rounded-md font-medium text-primary-600 hover:text-primary-500 focus-within:outline-none focus-within:ring-2 focus-within:ring-offset-2 focus-within:ring-primary-500">
                      <span>Télécharger un fichier</span>
                      <input id="file" name="file" type="file" accept=".pdf" class="sr-only" />
                    </label>
                    <p class="pl-1">ou glisser-déposer</p>
                  </div>
                  <p class="text-xs text-neutral-500">PDF jusqu'à 10MB</p>
                </div>
              </div>
              <p class="text-xs text-neutral-500 mt-2">Formats acceptés : PDF uniquement. Taille maximale : 10MB</p>
            </div>

            <!-- Actions -->
            <div class="flex items-center justify-end gap-4 pt-6 border-t border-neutral-200">
              <a href="{{route('admin.memories.index')}}" class="px-4 py-2 text-sm text-neutral-600 hover:text-neutral-900 border border-neutral-300 rounded-md transition-colors">
                Annuler
              </a>
              <button type="submit" class="inline-flex items-center gap-2 px-6 py-2 bg-primary-600 text-white rounded-md hover:bg-primary-700 transition-colors">
                <i class="fa-solid fa-save"></i>
                Créer le mémoire
              </button>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>
@endslot

@end
