@layout.admin({ title: "Gestion des mémoires" })

@slot('content')
  <div class="min-h-screen bg-neutral-50">
    <!-- Header -->
    <header class="bg-white border-b border-neutral-200">
      <div class="container mx-auto px-4 py-4">
        <div class="flex items-center justify-between">
          <div>
            <h1 class="text-2xl font-bold text-neutral-900">Gestion des mémoires</h1>
            <p class="text-neutral-600">Liste de tous les mémoires de la bibliothèque</p>
          </div>
          <div class="flex items-center gap-3">
            <a href="{{route('admin.dashboard')}}" class="text-sm text-neutral-600 hover:text-primary-600 transition-colors">
              <i class="fa-solid fa-arrow-left mr-1"></i>
              Retour au dashboard
            </a>
            <a href="{{route('admin.memories.create')}}" class="inline-flex items-center gap-2 px-4 py-2 bg-primary-600 text-white rounded-md hover:bg-primary-700 transition-colors">
              <i class="fa-solid fa-plus"></i>
              Nouveau mémoire
            </a>
          </div>
        </div>
      </div>
    </header>

    <div class="container mx-auto px-4 py-8">
      <!-- Flash Messages -->
      @if(flashMessages.get('success'))
        <div class="mb-6 rounded-md border border-success-200 bg-success-50 text-success-700 p-4">
          <div class="flex">
            <i class="fa-solid fa-check-circle text-success-400 mr-3"></i>
            <div>{{ flashMessages.get('success') }}</div>
          </div>
        </div>
      @endif

      @if(flashMessages.get('error'))
        <div class="mb-6 rounded-md border border-error-200 bg-error-50 text-error-700 p-4">
          <div class="flex">
            <i class="fa-solid fa-exclamation-triangle text-error-400 mr-3"></i>
            <div>{{ flashMessages.get('error') }}</div>
          </div>
        </div>
      @endif

      <!-- Memories Table -->
      <div class="bg-white rounded-lg border border-neutral-200 overflow-hidden">
        <div class="px-6 py-4 border-b border-neutral-200">
          <h3 class="text-lg font-semibold text-neutral-900">Mémoires ({{ memories.length || 0 }})</h3>
        </div>

        @if(memories && memories.length > 0)
          <div class="overflow-x-auto">
            <table class="min-w-full divide-y divide-neutral-200">
              <thead class="bg-neutral-50">
                <tr>
                  <th class="px-6 py-3 text-left text-xs font-medium text-neutral-500 uppercase tracking-wider">Titre</th>
                  <th class="px-6 py-3 text-left text-xs font-medium text-neutral-500 uppercase tracking-wider">Auteurs</th>
                  <th class="px-6 py-3 text-left text-xs font-medium text-neutral-500 uppercase tracking-wider">Année</th>
                  <th class="px-6 py-3 text-left text-xs font-medium text-neutral-500 uppercase tracking-wider">Filière</th>
                  <th class="px-6 py-3 text-left text-xs font-medium text-neutral-500 uppercase tracking-wider">Actions</th>
                </tr>
              </thead>
              <tbody class="bg-white divide-y divide-neutral-200">
                @each(memory in memories)
                  <tr class="hover:bg-neutral-50">
                    <td class="px-6 py-4 whitespace-nowrap">
                      <div class="text-sm font-medium text-neutral-900">{{ memory.title }}</div>
                      @if(memory.keywords)
                        <div class="text-xs text-neutral-500 mt-1">
                          {{ memory.keywords.split(',').slice(0, 3).join(', ') }}{{ memory.keywords.split(',').length > 3 ? '...' : '' }}
                        </div>
                      @endif
                    </td>
                    <td class="px-6 py-4 whitespace-nowrap text-sm text-neutral-900">
                      {{ memory.authors || 'Non spécifié' }}
                    </td>
                    <td class="px-6 py-4 whitespace-nowrap text-sm text-neutral-900">
                      {{ memory.year || 'Non spécifié' }}
                    </td>
                    <td class="px-6 py-4 whitespace-nowrap">
                      @if(memory.filiere)
                        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-primary-100 text-primary-800">
                          {{ memory.filiere }}
                        </span>
                      @else
                        <span class="text-sm text-neutral-500">Non spécifié</span>
                      @endif
                    </td>
                    <td class="px-6 py-4 whitespace-nowrap text-sm font-medium">
                      <div class="flex items-center gap-2">
                        <a href="{{route('admin.memories.show', { id: memory.id })}}" class="text-primary-600 hover:text-primary-900">
                          <i class="fa-solid fa-eye"></i>
                        </a>
                        <a href="{{route('admin.memories.edit', { id: memory.id })}}" class="text-accent-600 hover:text-accent-900">
                          <i class="fa-solid fa-edit"></i>
                        </a>
                        <form action="{{route('admin.memories.destroy', { id: memory.id })}}" method="POST" class="inline" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce mémoire ?')">
                          {{ csrfField() }}
                          <button type="submit" class="text-error-600 hover:text-error-900">
                            <i class="fa-solid fa-trash"></i>
                          </button>
                        </form>
                      </div>
                    </td>
                  </tr>
                @endeach
              </tbody>
            </table>
          </div>


        @else
          <div class="px-6 py-12 text-center">
            <div class="text-neutral-400 mb-4">
              <i class="fa-solid fa-book text-6xl"></i>
            </div>
            <h3 class="text-xl font-semibold text-neutral-700 mb-2">Aucun mémoire trouvé</h3>
            <p class="text-neutral-600 mb-6">Commencez par ajouter votre premier mémoire à la bibliothèque.</p>
            <a href="{{route('admin.memories.create')}}" class="inline-flex items-center gap-2 px-4 py-2 bg-primary-600 text-white rounded-md hover:bg-primary-700 transition-colors">
              <i class="fa-solid fa-plus"></i>
              Ajouter un mémoire
            </a>
          </div>
        @endif
      </div>
    </div>
  </div>
@endslot

@end
