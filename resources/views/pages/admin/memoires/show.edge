@layout.admin({ title: memory.title })

@slot('content')
  <div class="min-h-screen bg-neutral-50">
    <!-- Header -->
    <header class="bg-white border-b border-neutral-200">
      <div class="container mx-auto px-4 py-4">
        <div class="flex items-center justify-between">
          <div>
            <h1 class="text-2xl font-bold text-neutral-900">{{ memory.title }}</h1>
            <p class="text-neutral-600">Détails du mémoire</p>
          </div>
          <div class="flex items-center gap-3">
            <a href="{{route('admin.memories.index')}}" class="text-sm text-neutral-600 hover:text-primary-600 transition-colors">
              <i class="fa-solid fa-arrow-left mr-1"></i>
              Retour à la liste
            </a>
            <a href="{{route('admin.memories.edit', { id: memory.id })}}" class="inline-flex items-center gap-2 px-4 py-2 bg-accent-600 text-white rounded-md hover:bg-accent-700 transition-colors">
              <i class="fa-solid fa-edit"></i>
              Modifier
            </a>
          </div>
        </div>
      </div>
    </header>

    <div class="container mx-auto px-4 py-8">
      <div class="max-w-4xl mx-auto">
        <!-- Flash Messages -->
        @if(flashMessages.get('success'))
          <div class="mb-6 rounded-md border border-success-200 bg-success-50 text-success-700 p-4">
            <div class="flex">
              <i class="fa-solid fa-check-circle text-success-400 mr-3"></i>
              <div>{{ flashMessages.get('success') }}</div>
            </div>
          </div>
        @endif

        <!-- Memory Details -->
        <div class="bg-white rounded-lg border border-neutral-200 overflow-hidden">
          <!-- Header -->
          <div class="px-6 py-4 border-b border-neutral-200 bg-neutral-50">
            <div class="flex items-center justify-between">
              <h2 class="text-lg font-semibold text-neutral-900">Informations du mémoire</h2>
              <div class="flex items-center gap-2">
                <span class="text-xs text-neutral-500">ID: {{ memory.id }}</span>
                <span class="text-xs text-neutral-500">Créé le: {{ memory.createdAt.toFormat('dd/MM/yyyy') }}</span>
              </div>
            </div>
          </div>

          <!-- Content -->
          <div class="p-6 space-y-6">
            <!-- Title -->
            <div>
              <h3 class="text-sm font-medium text-neutral-500 uppercase tracking-wider mb-2">Titre</h3>
              <p class="text-lg font-medium text-neutral-900">{{ memory.title }}</p>
            </div>

            <!-- Authors and Year -->
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
              <div>
                <h3 class="text-sm font-medium text-neutral-500 uppercase tracking-wider mb-2">Auteurs</h3>
                <p class="text-neutral-900">{{ memory.authors || 'Non spécifié' }}</p>
              </div>
              <div>
                <h3 class="text-sm font-medium text-neutral-500 uppercase tracking-wider mb-2">Année</h3>
                <p class="text-neutral-900">{{ memory.year || 'Non spécifié' }}</p>
              </div>
            </div>

            <!-- Filiere and Keywords -->
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
              <div>
                <h3 class="text-sm font-medium text-neutral-500 uppercase tracking-wider mb-2">Filière</h3>
                @if(memory.filiere)
                  <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-primary-100 text-primary-800">
                    {{ memory.filiere }}
                  </span>
                @else
                  <p class="text-neutral-500">Non spécifié</p>
                @endif
              </div>
              <div>
                <h3 class="text-sm font-medium text-neutral-500 uppercase tracking-wider mb-2">Mots-clés</h3>
                @if(memory.keywords)
                  <div class="flex flex-wrap gap-2">
                    @each(keyword in memory.keywords.split(',').map(k => k.trim()))
                      <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-neutral-100 text-neutral-800">
                        {{ keyword }}
                      </span>
                    @endeach
                  </div>
                @else
                  <p class="text-neutral-500">Aucun mot-clé</p>
                @endif
              </div>
            </div>

            <!-- Abstract -->
            <div>
              <h3 class="text-sm font-medium text-neutral-500 uppercase tracking-wider mb-2">Résumé / Abstract</h3>
              @if(memory.abstract)
                <div class="bg-neutral-50 rounded-md p-4">
                  <p class="text-neutral-700 leading-relaxed">{{ memory.abstract }}</p>
                </div>
              @else
                <p class="text-neutral-500">Aucun résumé disponible</p>
              @endif
            </div>

            <!-- File Path -->
            <div>
              <h3 class="text-sm font-medium text-neutral-500 uppercase tracking-wider mb-2">Fichier</h3>
              @if(memory.filePath)
                <div class="flex items-center gap-3">
                  <a href="{{ memory.filePath }}" target="_blank" class="inline-flex items-center gap-2 px-4 py-2 bg-primary-600 text-white rounded-md hover:bg-primary-700 transition-colors">
                    <i class="fa-solid fa-file-pdf"></i>
                    Consulter le PDF
                  </a>
                  <span class="text-sm text-neutral-600">{{ memory.filePath }}</span>
                </div>
              @else
                <p class="text-neutral-500">Aucun fichier associé</p>
              @endif
            </div>

            <!-- Timestamps -->
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6 pt-6 border-t border-neutral-200">
              <div>
                <h3 class="text-sm font-medium text-neutral-500 uppercase tracking-wider mb-2">Créé le</h3>
                <p class="text-neutral-900">{{ memory.createdAt.toFormat('dd/MM/yyyy à HH:mm') }}</p>
              </div>
              <div>
                <h3 class="text-sm font-medium text-neutral-500 uppercase tracking-wider mb-2">Modifié le</h3>
                <p class="text-neutral-900">{{ memory.updatedAt ? memory.updatedAt.toFormat('dd/MM/yyyy à HH:mm') : 'Jamais modifié' }}</p>
              </div>
            </div>
          </div>

          <!-- Actions Footer -->
          <div class="px-6 py-4 border-t border-neutral-200 bg-neutral-50">
            <div class="flex items-center justify-between">
              <div class="flex items-center gap-3">
                <a href="{{route('admin.memories.index')}}" class="text-sm text-neutral-600 hover:text-neutral-900 transition-colors">
                  <i class="fa-solid fa-list mr-1"></i>
                  Voir tous les mémoires
                </a>
              </div>
              <div class="flex items-center gap-3">
                <a href="{{route('admin.memories.edit', { id: memory.id })}}" class="inline-flex items-center gap-2 px-4 py-2 bg-accent-600 text-white rounded-md hover:bg-accent-700 transition-colors">
                  <i class="fa-solid fa-edit"></i>
                  Modifier
                </a>
                <form action="{{route('admin.memories.destroy', { id: memory.id })}}" method="POST" class="inline" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce mémoire ? Cette action est irréversible.')">
                  {{ csrfField() }}
                  <button type="submit" class="inline-flex items-center gap-2 px-4 py-2 bg-error-600 text-white rounded-md hover:bg-error-700 transition-colors">
                    <i class="fa-solid fa-trash"></i>
                    Supprimer
                  </button>
                </form>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
@endslot

@end
