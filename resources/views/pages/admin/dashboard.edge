@layout.admin({ title: "Tableau de bord - Admin" })

@slot('content')
  <div class="min-h-screen bg-neutral-50">
    <!-- Header -->
    <header class="bg-white border-b border-neutral-200">
      <div class="container mx-auto px-4 py-4">
        <div class="flex items-center justify-between">
          <div>
            <h1 class="text-2xl font-bold text-neutral-900">Tableau de bord</h1>
            <p class="text-neutral-600">Gestion de la bibliothèque des mémoires UATM</p>
          </div>
          <div class="flex items-center gap-3">
            <span class="text-sm text-neutral-500">Connecté en tant que</span>
            <div class="flex items-center gap-2 px-3 py-2 rounded-md bg-primary-50 border border-primary-200">
              <div class="w-2 h-2 rounded-full bg-primary-600"></div>
              <span class="text-sm font-medium text-primary-700">{{ auth.user.fullName || auth.user.email }}</span>
              <span class="text-xs text-primary-600 bg-primary-100 px-2 py-1 rounded-full">Admin</span>
            </div>
            <form action="/logout" method="POST" class="inline">
              @csrf()
              <button type="submit" class="text-sm text-neutral-600 hover:text-primary-600 transition-colors">
                <i class="fa-solid fa-sign-out-alt"></i>
                <span class="ml-1">Déconnexion</span>
              </button>
            </form>
          </div>
        </div>
      </div>
    </header>

    <div class="container mx-auto px-4 py-8">
      <!-- Statistics Cards -->
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">
        <div class="bg-white rounded-lg border border-neutral-200 p-6">
          <div class="flex items-center">
            <div class="p-2 rounded-md bg-primary-100">
              <i class="fa-solid fa-book text-primary-600 text-xl"></i>
            </div>
            <div class="ml-4">
              <p class="text-sm font-medium text-neutral-600">Total Mémoires</p>
              <p class="text-2xl font-bold text-neutral-900">{{ stats.totalMemories || 0 }}</p>
            </div>
          </div>
        </div>

        <div class="bg-white rounded-lg border border-neutral-200 p-6">
          <div class="flex items-center">
            <div class="p-2 rounded-md bg-accent-100">
              <i class="fa-solid fa-users text-accent-600 text-xl"></i>
            </div>
            <div class="ml-4">
              <p class="text-sm font-medium text-neutral-600">Utilisateurs</p>
              <p class="text-2xl font-bold text-neutral-900">{{ stats.totalUsers || 0 }}</p>
            </div>
          </div>
        </div>

        <div class="bg-white rounded-lg border border-neutral-200 p-6">
          <div class="flex items-center">
            <div class="p-2 rounded-md bg-success-100">
              <i class="fa-solid fa-eye text-success-600 text-xl"></i>
            </div>
            <div class="ml-4">
              <p class="text-sm font-medium text-neutral-600">Consultations</p>
              <p class="text-2xl font-bold text-neutral-900">{{ stats.totalViews || 0 }}</p>
            </div>
          </div>
        </div>

        <div class="bg-white rounded-lg border border-neutral-200 p-6">
          <div class="flex items-center">
            <div class="p-2 rounded-md bg-warning-100">
              <i class="fa-solid fa-download text-warning-600 text-xl"></i>
            </div>
            <div class="ml-4">
              <p class="text-sm font-medium text-neutral-600">Téléchargements</p>
              <p class="text-2xl font-bold text-neutral-900">{{ stats.totalDownloads || 0 }}</p>
            </div>
          </div>
        </div>
      </div>

      <!-- Quick Actions -->
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 mb-8">
        <div class="bg-white rounded-lg border border-neutral-200 p-6">
          <h3 class="text-lg font-semibold text-neutral-900 mb-4">Actions rapides</h3>
          <div class="space-y-3">
            <a href="/admin/memories/create" class="flex items-center gap-3 p-3 rounded-md border border-neutral-200 hover:border-primary-300 hover:bg-primary-50 transition-colors">
              <div class="p-2 rounded-md bg-primary-100">
                <i class="fa-solid fa-plus text-primary-600"></i>
              </div>
              <div>
                <p class="font-medium text-neutral-900">Ajouter un mémoire</p>
                <p class="text-sm text-neutral-600">Créer une nouvelle entrée de mémoire</p>
              </div>
            </a>

            <a href="/admin/memories" class="flex items-center gap-3 p-3 rounded-md border border-neutral-200 hover:border-primary-300 hover:bg-primary-50 transition-colors">
              <div class="p-2 rounded-md bg-accent-100">
                <i class="fa-solid fa-list text-accent-600"></i>
              </div>
              <div>
                <p class="font-medium text-neutral-900">Gérer les mémoires</p>
                <p class="text-sm text-neutral-600">Voir, modifier ou supprimer des mémoires</p>
              </div>
            </a>

            <a href="/admin/users" class="flex items-center gap-3 p-3 rounded-md border border-neutral-200 hover:border-primary-300 hover:bg-primary-50 transition-colors">
              <div class="p-2 rounded-md bg-success-100">
                <i class="fa-solid fa-user-cog text-success-600"></i>
              </div>
              <div>
                <p class="font-medium text-neutral-900">Gérer les utilisateurs</p>
                <p class="text-sm text-neutral-600">Gérer les comptes administrateurs</p>
              </div>
            </a>
          </div>
        </div>

        <div class="bg-white rounded-lg border border-neutral-200 p-6">
          <h3 class="text-lg font-semibold text-neutral-900 mb-4">Mémoires récents</h3>
          <div class="space-y-3">
            @if(recentMemories && recentMemories.length > 0)
              @each(memory in recentMemories.slice(0, 5))
                <div class="flex items-start justify-between p-3 rounded-md bg-neutral-50">
                  <div class="flex-1">
                    <p class="font-medium text-neutral-900 text-sm">{{ memory.title }}</p>
                    <p class="text-xs text-neutral-600">{{ memory.authors }} — {{ memory.year }}</p>
                  </div>
                  <a href="/admin/memories/{{ memory.id }}/edit" class="text-xs text-primary-600 hover:text-primary-700">
                    <i class="fa-solid fa-edit"></i>
                  </a>
                </div>
              @endeach
            @else
              <p class="text-neutral-500 text-sm">Aucun mémoire disponible</p>
            @endif
          </div>
          <div class="mt-4">
            <a href="/admin/memories" class="text-sm text-primary-600 hover:text-primary-700">
              Voir tous les mémoires →
            </a>
          </div>
        </div>
      </div>

      <!-- System Status -->
      <div class="bg-white rounded-lg border border-neutral-200 p-6">
        <h3 class="text-lg font-semibold text-neutral-900 mb-4">État du système</h3>
        <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
          <div class="flex items-center gap-3">
            <div class="w-3 h-3 rounded-full bg-success-500"></div>
            <span class="text-sm text-neutral-700">Base de données Oracle</span>
          </div>
          <div class="flex items-center gap-3">
            <div class="w-3 h-3 rounded-full bg-success-500"></div>
            <span class="text-sm text-neutral-700">Serveur web</span>
          </div>
          <div class="flex items-center gap-3">
            <div class="w-3 h-3 rounded-full bg-success-500"></div>
            <span class="text-sm text-neutral-700">Stockage fichiers</span>
          </div>
        </div>
      </div>
    </div>
  </div>
@endslot

@end
