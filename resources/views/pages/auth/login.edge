@layout.main({ title: "Connexion admin" })

@slot('content')
  <div class="min-h-[70vh] flex items-center">
    <div class="container mx-auto">
      @include('components/header')

      <div class="max-w-md mx-auto mt-10 bg-white border border-neutral-200 rounded-xl shadow-sm p-6">
        <h1 class="text-2xl font-bold text-neutral-900 mb-1">Connexion administrateur</h1>
        <p class="text-sm text-neutral-600 mb-6">Accès réservé aux administrateurs de la bibliothèque.</p>

        @if(flashMessages.get('errors'))
          <div class="mb-4 rounded-md border border-error-200 bg-error-50 text-error-700 p-3 text-sm">
            <ul class="list-disc pl-5">
              @each(error in Object.values(flashMessages.get('errors')))
                <li>{{ error }}</li>
              @endeach
            </ul>
          </div>
        @endif

        <form action="/login" method="POST" class="space-y-4">
          @csrf()
          <div>
            <label class="block text-sm font-medium text-neutral-700 mb-1">Email</label>
            <input type="email" name="email" required class="w-full rounded-md border border-neutral-300 px-3 py-2 focus:outline-none focus:ring-2 focus:ring-primary-300 focus:border-primary-500" />
          </div>
          <div>
            <div class="flex items-center justify-between">
              <label class="block text-sm font-medium text-neutral-700 mb-1">Mot de passe</label>
              <a href="#" class="text-xs text-primary-600">Mot de passe oublié ?</a>
            </div>
            <input type="password" name="password" required class="w-full rounded-md border border-neutral-300 px-3 py-2 focus:outline-none focus:ring-2 focus:ring-primary-300 focus:border-primary-500" />
          </div>

          <button type="submit" class="w-full inline-flex items-center justify-center gap-2 px-4 py-2 rounded-md bg-primary-600 hover:bg-primary-700 text-white font-semibold">
            <i class="fa-solid fa-right-to-bracket"></i>
            Se connecter
          </button>
        </form>
      </div>

      @include('components/footer')
    </div>
  </div>
@endslot

@end

