@eval(await auth.check())
<header class="w-full bg-white/90 backdrop-blur border-b border-neutral-200">
  <div class="container mx-auto">
    <div class="flex items-center justify-between py-4">
      <a href="/" class="flex items-center gap-3 group">
        <img src="/images/logo/biblio-web-uatm.png" alt="Biblio-Web UATM" class="h-10 w-10 rounded" />
        <div class="leading-tight">
          <div class="text-lg font-semibold text-primary-700 group-hover:text-primary-600 transition-colors">Biblio-Web UATM</div>
          <div class="text-xs text-neutral-500">Bibliothèque des mémoires</div>
        </div>
      </a>

      <nav class="hidden md:flex items-center gap-6 text-sm">
        <a href="/#recherche" class="text-neutral-600 hover:text-primary-600 transition-colors">Recherche</a>
        <a href="/search" class="text-neutral-600 hover:text-primary-600 transition-colors">Recherche avancée</a>
        {{-- <a href="#thematiques" class="text-neutral-600 hover:text-primary-600 transition-colors">Thématiques</a>
        <a href="#nouveautes" class="text-neutral-600 hover:text-primary-600 transition-colors">Nouveautés</a> --}}
        {{-- <a href="#contact" class="text-neutral-600 hover:text-primary-600 transition-colors">Contact</a> --}}
      </nav>

      <div class="flex items-center gap-3">
        @if(auth.isAuthenticated)
          <div class="flex items-center gap-3">
            <div class="flex items-center gap-2 px-3 py-2 rounded-md bg-primary-50 border border-primary-200">
              <div class="w-2 h-2 rounded-full bg-primary-600"></div>
              <span class="text-sm font-medium text-primary-700">{{ auth.user.fullName || auth.user.email }}</span>
              <span class="text-xs text-primary-600 bg-primary-100 px-2 py-1 rounded-full">Admin</span>
            </div>
            <form action="{{route('auth.logout')}}" method="POST" class="inline">
              {{ csrfField() }}
              <button type="submit" class="text-sm text-neutral-600 hover:text-primary-600 transition-colors">
                <i class="fa-solid fa-sign-out-alt"></i>
                <span class="hidden sm:inline ml-1">Déconnexion</span>
              </button>
            </form>
          </div>
        @else
          <a href="{{route('login.page')}}" class="hidden sm:inline-flex items-center gap-2 px-3 py-2 rounded-md bg-primary-600 text-white text-sm hover:bg-primary-700 transition-colors">
            <i class="fa-solid fa-right-to-bracket text-xs"></i>
            <span>Se connecter</span>
          </a>
        @endif
      </div>
    </div>
  </div>
</header>
